# Sample DITA Project

This small illustrative project demonstrates:

- a root map
- a key definition
- a key reference
- a reusable content fragment
- a conref
- a DITAVAL exclusion

It is intentionally small enough to inspect by hand. It is not a conformance suite.

Root map: [`maps/product.ditamap`](maps/product.ditamap)
